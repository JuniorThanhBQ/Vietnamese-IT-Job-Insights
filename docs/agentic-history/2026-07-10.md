# Agentic Activity Log - 2026-07-10

## 1. Requested Task
* Read and understand the project context to propose a scraping pipeline architecture for ITviec & TopDev.
* Optimize configurations for the 8GB RAM development system running Windows 11.
* Replace legacy `requirements.txt` files and migrate to centralized dependency management using `pyproject.toml`.
* Rename the directory `crawler/core` to `crawler/base` to avoid naming conflicts with core business logic.
* Setup `AGENTS.md` at root specifying agent guidelines and rules.
* Create a dedicated `backend/AGENTS.md` specifying layered-by-module backend rules, and set up `import-linter` and `pylint` for checks.

## 2. Applied Solution
* Integrated all dependencies (`dev`, `crawler`) under PEP 621 `optional-dependencies` inside `backend/pyproject.toml`.
* Configured unified environment loading using `pydantic-settings` to load from `.env` or parent `.env`.
* Configured lightweight Redis and Celery limits to protect CPU/RAM resources.
* Installed Python environment dependencies and Playwright Chromium in the root virtual environment.
* Configured `import-linter` and `pylint` (moderate strictness) in `backend/pyproject.toml` and verified local execution.
* Approved `docs/adrs/ADR-03-Database-Design-and-Vector-Search-Strategy.md` and set up the local infrastructure.
* Configured async database connection and session management in the backend.
* Created the backend modules skeleton (`companies` and `jobs`) and registered routers in the main application.

## 3. Actions in Repository
* **Deleted:** [backend/requirements.txt](../../backend/requirements.txt) (legacy).
* **Updated:** [backend/pyproject.toml](../../backend/pyproject.toml) (Added `dev` & `crawler` package groups, corrected versions of `python-jose` and `passlib`, added `import-linter` and `pylint` configs/deps, disabled Pylint R0917).
* **Updated:** [backend/app/config.py](../../backend/app/config.py) (Implemented Settings class with environment loading).
* **Created:** [.env](../../.env) (Setup staging connection strings).
* **Renamed:** Directory `crawler/core` to [crawler/base](../../crawler/base).
* **Created:** [crawler/config/settings.py](../../crawler/config/settings.py) (Shared backend configuration loading) and modules initialization files.
* **Installed:** Package dependencies and Playwright Chromium binary.
* **Created:** [AGENTS.md](../../AGENTS.md) (Agent guidelines and instructions).
* **Created:** [backend/AGENTS.md](../../backend/AGENTS.md) (Backend-specific layered-by-module specifications, linter, pylint, and commenting standards).
* **Updated:** [docs/adrs/ADR-03-Database-Design-and-Vector-Search-Strategy.md](../../docs/adrs/ADR-03-Database-Design-and-Vector-Search-Strategy.md) (Approved status and date).
* **Created:** [docker-compose.local.yml](../../docker-compose.local.yml) (Configured lightweight PostgreSQL with pgvector and Redis alpine).
* **Created:** [backend/app/models/database.py](../../backend/app/models/database.py) (Async database connection engine, session maker, get_db dependency, and init_db helper).
* **Created:** Directory structure and files under `backend/app/modules/` for `companies` and `jobs` (implementing `models.py`, `repository.py`, `services.py`, `views.py`, and `routers.py` for each).
* **Updated:** [backend/app/main.py](../../backend/app/main.py) (Registered module routers under `/api/v1` prefix and added lifespan context manager for async database initialization on startup).
* **Created:** [scripts/export_openapi.py](../../scripts/export_openapi.py) (Python script to generate static OpenAPI JSON specs).
* **Created:** [docs/apis/api_v1.json](../../docs/apis/api_v1.json) (Exported FastAPI routes schemas for API documentation).
* **Updated:** [backend/app/modules/jobs/repository.py](../../backend/app/modules/jobs/repository.py) (Fixed Ruff E712 comparison check for `is_active` boolean column).
